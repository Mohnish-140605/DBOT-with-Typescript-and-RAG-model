'use client'

export function HydrationDebugger() {
  // Removed debug fetch calls that were causing connection errors
  // This component is kept for potential future debugging needs
  return null
}
